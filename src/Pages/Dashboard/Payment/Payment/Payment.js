import React from 'react';
import { Alert, Container } from 'react-bootstrap';

const Payment = () => {
    return (
        <Container>
            <Alert variant='warning' className='title'>We are working on it.It will be available very soon</Alert>
        </Container>
    );
};

export default Payment;